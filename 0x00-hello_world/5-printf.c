#include <stdio.h>
/**
 * main - A program to print a line using the printf function
 * Return: 0(this means that the program is successful)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
